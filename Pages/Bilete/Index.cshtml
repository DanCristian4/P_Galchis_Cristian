@page
@model P_Galchis_Cristian.Pages.Bilete.IndexModel

@{
    ViewData["Title"] = "Bilete";
}

<h1>Bilete</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bilet[0].Client)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bilet[0].Obiectiv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bilet[0].Data)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Bilet) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Client.NumeComplet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Obiectiv.Denumire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
